<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Histórico de Orçamentos</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/_sidebar', { currentPage: 'historico' }) %>

    <div class="main-content">
        <%- include('partials/_header') %>
        
        <main class="container-fluid p-4">
            <h1 class="mb-4">Histórico</h1>
            <div class="card shadow-sm">
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead><tr><th>O.S.</th><th>Defeito</th><th>Data</th><th class="text-end">Ações</th></tr></thead>
                            <tbody>
                                <% if (budgets.length > 0) { %>
                                    <% budgets.forEach(budget => { %>
                                    <tr>
                                        <td><%= budget.os %></td>
                                        <td><%= budget.defeito %></td>
                                        <td><%= new Date(budget.date).toLocaleDateString('pt-BR') %></td>
                                        <td class="text-end">
                                            <a href="/data/<%= budget.pdfFile %>" class="btn btn-primary btn-sm" target="_blank">Ver PDF</a>
                                            <a href="/delete/<%= budget.os %>/<%= budget.pdfFile %>" class="btn btn-danger btn-sm" onclick="return confirm('Tem certeza?');">Excluir</a>
                                        </td>
                                    </tr>
                                    <% }); %>
                                <% } else { %>
                                    <tr><td colspan="4" class="text-center">Nenhum orçamento encontrado.</td></tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </main>
    </div>
    
    <script src="/js/script.js"></script>
</body>
</html>